# Puppeteer E2E Tests

Este contenedor ejecuta pruebas automatizadas sobre el frontend.

## Ejecutar las pruebas

1. Asegúrate de que `frontend` y `api` están levantados:

```bash
docker compose up -d frontend api
